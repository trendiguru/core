- hosts: braini
  gather_facts: no
  tasks:
#  - debug: msg="hi"
  - ping:

- hosts: gcloud_ubuntu_user
  gather_facts: no
  tasks:
#  - debug: msg="hi"
  - ping:

- hosts: gcloud_root_user
  gather_facts: no
  tasks:
  #- debug: msg="hi"
  - ping:
